# Cauldron.Abstractions

Shared service interfaces (`ICauldronService` and friends) implemented by
CauldronServer and consumed by the tools/launcher.

Interface-only contracts shared across the Cauldron projects.
